{
  "name": "root",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run --cwd packages/rehype-custom-code",
    "check": "bunx @biomejs/biome check --apply ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.5"
  }
}
